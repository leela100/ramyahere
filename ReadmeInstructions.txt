1) BUILD

To preview:

hugo server



To build (normal)

Hugo

To clean and build:

hugo --gc --minify


2) Commit to Git

git add .
git commit -m "MESSAGE"


3) Push to Github

git push origin main

ALL GIT Code together: copy paste by editing message

git add .
git commit -m "Minor updates to content"
git push origin main


-----

To check if git is connected:

git remote -v


(Should return with my repo)


